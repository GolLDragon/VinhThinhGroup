<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vinhthinhgroup">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconandroid"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ChiTietDoDung"></activity>
        <activity android:name=".activity.ChonDoDung" />
        <activity android:name=".activity.CongSuatTong" />
        <activity android:name=".activity.ChonPhong" />
        <activity android:name=".activity.Anh" />
        <activity android:name=".activity.LoaiSPCuaTB" />
        <activity android:name=".activity.ThietBi" />
        <activity android:name=".activity.ThongTinKhachHang" />
        <activity android:name=".activity.GioHang" />
        <activity android:name=".activity.ChiTietSP" />
        <activity android:name=".activity.LienHe" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".activity.ChiNhanhKhac" />
        <activity
            android:name=".activity.DiaChi"
            android:label="@string/title_activity_dia_chi" />
        <activity android:name=".activity.SP" />
        <activity android:name=".activity.LoaiSP" />
        <activity android:name=".activity.NhomSP" />
        <activity android:name=".activity.TrangChu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>